# 🎵 Tempo

[English](README.md) · **Русский**

**Локальный десктопный музыкальный плеер.** Ваши файлы, ваша библиотека — без аккаунтов и облака, всё работает офлайн.

Собран на **Tauri 2 + React 18 + TypeScript** во фронтенде и **Rust + SQLite** под капотом. Ни Electron, ни бэкенд-сервера, ни телеметрии.

![Version](https://img.shields.io/badge/version-0.1.0--alpha-blue) ![Tauri](https://img.shields.io/badge/Tauri-2-FFC131?logo=tauri&logoColor=black) ![React](https://img.shields.io/badge/React-18-61DAFB?logo=react&logoColor=black) ![TypeScript](https://img.shields.io/badge/TypeScript-strict-3178C6?logo=typescript&logoColor=white) ![Rust](https://img.shields.io/badge/Rust-2021-DEA584?logo=rust&logoColor=black) ![SQLite](https://img.shields.io/badge/SQLite-WAL-003B57?logo=sqlite&logoColor=white)

<!-- Скриншот: положите PNG (например, docs/screenshot.png) и раскомментируйте:
![Скриншот Tempo](docs/screenshot.png)
-->

## ✨ Возможности

- **Локальная библиотека** — укажите Tempo папки с музыкой (MP3, FLAC, M4A, AAC, OGG, Opus, WAV), и он соберёт из них коллекцию: альбомы, исполнители, треки.
- **Быстрое инкрементальное сканирование** — теги читаются через `lofty`, обложки альбомов извлекаются в каталог данных приложения. Неизменённые файлы (тот же размер + mtime) пропускаются, поэтому повторное сканирование почти мгновенное. Сканирование выполняется в потоках Rust и никогда не блокирует интерфейс.
- **Полноценный движок воспроизведения** — очередь с шаффлом, повтором (выкл / все / один трек), перемоткой и громкостью; счётчики прослушиваний и история записываются автоматически.
- **Плейлисты** — создание, переупорядочивание, переименование; треки добавляются из любого места приложения.
- **Тексты песен** — синхронизированная лирика из встроенных тегов или `.lrc`-файлов, с онлайн-фолбэком и аккуратным полноэкранным оверлеем.
- **Провайдер SoundCloud** — поиск и стриминг с SoundCloud (HLS через `hls.js`) рядом с локальной библиотекой — через единую абстракцию провайдеров.
- **Двуязычный интерфейс** — английский и русский из коробки.
- **Настройка под себя** — тёмная плоская тема с настраиваемыми акцентными цветами и опциональным размытым фоновым изображением.

## 🖥️ Экраны

Главная · Библиотека · Альбомы · Исполнители · Плейлисты · История · Поиск · Настройки — плюс постоянная панель плеера и панель очереди.

## 🧱 Технологии

| Слой | Технологии |
|---|---|
| Оболочка | Tauri 2 (без Electron) |
| Интерфейс | React 18 + TypeScript (strict) + Vite 6, иконки `lucide-react`, собственный CSS (без фреймворков) |
| Воспроизведение | Синглтон HTML5 `<audio>` + чистый контроллер очереди (шаффл-перестановка, режимы повтора) |
| Бэкенд | Rust: `rusqlite` (bundled SQLite, режим WAL), `walkdir`, `lofty`, `reqwest` |
| Данные | Один файл SQLite в каталоге данных приложения; упорядоченные миграции через `user_version` |
| Поиск | SQL `LIKE`-запросы по трекам / альбомам / исполнителям, постраничные списки (500 на страницу) |

## 🚀 Начало работы

**Требования:** [Node.js 18+](https://nodejs.org), [Rust](https://rustup.rs) и [предварительные требования Tauri 2](https://tauri.app/start/prerequisites/) для вашей платформы.

```bash
# установить зависимости
npm install

# запуск в режиме разработки
npm run tauri dev

# сборка релизного установщика (Windows: NSIS .exe)
npm run tauri build
```

Фронтенд можно разрабатывать и отдельно (`npm run dev`) — Vite с горячей перезагрузкой на порту 1420.

## 📁 Структура проекта

```
src/                  # React-фронтенд
  api/                #   типизированные обёртки над командами Tauri + подписки на события
  components/         #   общие UI-элементы (списки треков, обложки, модальные окна, панель плеера, очередь)
  features/lyrics/    #   провайдеры лирики, разбор LRC, оверлей
  pages/              #   Главная, Библиотека, Альбомы, Исполнители, Плейлисты, История, Поиск, Настройки
  player/             #   движок воспроизведения: контроллер, очередь, React-биндинги
  providers/          #   абстракция источников музыки (локальные файлы, SoundCloud)
  i18n/               #   переводы EN / RU
src-tauri/            # бэкенд на Rust
  src/database.rs     #   слой SQLite + миграции
  src/scanner.rs      #   обход файловой системы + инкрементальное сканирование
  src/metadata.rs     #   извлечение тегов и обложек (lofty)
  src/commands.rs     #   команды Tauri (Result<T, String>)
```

Полная архитектура и контракты модулей описаны в [ARCHITECTURE.md](ARCHITECTURE.md).

## 🗺️ Статус

`v0.1.0` — ранняя альфа. Воспроизведение, сканирование библиотеки, плейлисты, лирика и поиск уже работают, но возможны шероховатости.

## 📄 Лицензия

Проект распространяется по лицензии [MIT](LICENSE).
